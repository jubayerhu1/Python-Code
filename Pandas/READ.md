# 🐼 Data Analysis with Pandas

A lightweight starter project for data manipulation, cleaning, and analysis using the powerful **Pandas** library in Python.

---

## 📝 Short Description
**Pandas** is the "Excel for Python." It is an open-source library providing high-performance, easy-to-use data structures (DataFrames) and data analysis tools for labeled data. Whether you are cleaning messy CSV files or preparing data for Machine Learning, Pandas is the go-to tool.

## ✨ Why is it Useful?
* 🧹 **Data Cleaning:** Easily handle missing data (`NaN`), duplicates, and outliers.
* 📊 **Flexible Loading:** Read/Write data from CSV, Excel, SQL, JSON, and more.
* ⚡ **High Performance:** Built on top of NumPy, making it incredibly fast for large datasets.
* 🔄 **Data Wrangling:** Merge, join, pivot, and reshape datasets with single-line commands.

---

## ⚙️ Environment Setup

Follow these steps to get your development environment ready:

### 1. Create a Virtual Environment
It is recommended to use a virtual environment to avoid library conflicts.
```bash
# Create the environment
```
conda create -n env_name(jub-l) python=3.12 -y
```

# Activate (Windows)
```
conda activate env_name(jub-l)
``` 

### Installment requirement 
    - pandas
    -numpy
    -pyarrow
    -openpyxl
```
pip install -r ./requirements.txt
```





🛠 Project Structure
📂 data/ - Raw and processed datasets.

📂 notebooks/ - Jupyter notebooks for exploration.

📄 main.py - Core data processing script.

📄 requirements.txt - List of project dependencies.